export interface AssignmentRequest {
    beginDate: string,
    endDate: string,
    beginTime: string,
    endTime: string,
    dayOfWeek: number, 
    subjectId: number, 
    lecturerId: number,
    classroomId: number
}